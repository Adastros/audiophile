import { Box } from '@chakra-ui/react';
import MenuIconButton from './MenuIconButton';

const Navigation = () => {
  return (
    <Box>
      <MenuIconButton />
    </Box>
  );
};

export default Navigation;
